;; production environment overrides for the docker container
{
 :tpximpact.datahost.ldapi.jetty/runnable-service {"0.0.0.0" ;; bind to all network interfaces
                                                   :port 80}
 }
